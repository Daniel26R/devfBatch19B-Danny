"use strict";

// Capturando los nodos de html con javascript
const app = document.querySelector(".app");

const input_todo = document.getElementById("input-todo");
const btn_add = document.getElementById("btn-add");
const content_w = document.getElementById("content-wrapper");

const btn_background = document.querySelector(".btn-background");
